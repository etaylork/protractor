import { by, ele } from '../../../utils';

export class DispatchShipmentPage {
    dispatchShipmentButton = ele(by.id("shipment_generic_submit_btn"));
    backToShipmentsButton = ele(by.id("back_to_shipments_btn"));
}